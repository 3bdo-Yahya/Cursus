# Hero live demo — Impact Analyzer (Beat 3)

Exactly **one** live app moment. Everything else stays on the WebGL stage.

## Credentials

| | |
|---|---|
| URL | Localhost app (`dotnet run --project Cursus.PL` from `src/`) |
| Email | `mazen.hassan@cursus.demo` |
| Password | `Demo123!` |
| Course | **CS211** Data Structures I |

## Rehearsed path (Abdo drives · Esraa narrates)

1. Pre-login before Beat 3 starts (or during architecture).
2. After WebGL cascade (**C**): Alt-Tab to browser.
3. Course Map → select **CS211** → **Simulate Failure**.
4. Show cascade + impact drawer KPIs.
5. **View Full Report** — scenario, 6 KPIs, blocked list, recovery timeline, Summer option, recommendations.
6. Hand back to WebGL → Beat 4 (GPA).

## KPI check (must match stage / CASCADE_KPIS.md)

- [ ] Blocked ≈ **20**
- [ ] Credits at risk ≈ **60**
- [ ] Note live graduation delay + projected labels; update `CASCADE_KPIS.md` if they differ

## Fallback video (record before defense day)

Record 60–90s screen capture of the same path above. Filename suggestion:

`docs/presentation/assets/mazen-cs211-impact-fallback.mp4`

If localhost fails (Wi-Fi, DB, GPU): Esraa continues the Beat 3 script over the video — same numbers, same beats.

## Contingency order

1. Live Mazen localhost  
2. Fallback video  
3. WebGL cascade alone (numbers still on panel)

## Do not live-demo (in this outline)

GPA Simulator, AI Advisor, Admin, Super Admin — narrated on WebGL only (reduces failure surface).
